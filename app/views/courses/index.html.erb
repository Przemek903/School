<div class="container-fluid">
	<h2>Aktualne kursy:</h2>
	<% @courses.each do |c| %>
	<div class="row">
		<div class="col-md-1"></div>
		<div class="col-md-10">
				<div class="col-md-2"><%= image_tag("test.jpg", size: "150x100") %></div>
				<div class="col-md-8">
					<p><%= c.name%></p>
					<p><%= c.description[0...100] %> ...</p>
					<p><i> <%= c.user.name %> <%= c.user.surname %></i></p>
				</div>
				<div class="col-md-2"><%= link_to "Sprawdź kurs", course_path(c.id), class: "btn btn-info" %></div>
		</div>
		<div class="col-md-1"></div>
	</div>
	<% end %>
</div>
